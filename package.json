{
  "name": "starburst-saas-bot",
  "version": "1.0.0",
  "description": "Telegram SaaS Bot with Star Payments",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "telegraf": "^4.16.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
